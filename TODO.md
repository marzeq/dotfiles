# Notifications

Bring back notifications under the shell instead of using `swaync`.

This time with proper thought for memory usage and performance, not like the old one which caused constant leaks.
